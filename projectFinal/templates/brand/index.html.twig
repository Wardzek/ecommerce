{% extends 'base.html.twig' %}

{% block title %}Hello BrandController!{% endblock %}

{% block main %}
    <div class="container">
        <h1 class="mt-3 mb-4">Nos marques</h1>
        <div class="row">
            {% for brand in brands %}
                <div class="col-md-4">
                    <div class="card mb-4">
                        {# {% if car.attachment != null %}
                            <img class="img" src="{{ asset('uploads/attachment/' ~ car.attachment) }}" alt="Vroum vrouuum">
                        {% else %}
                            <img class="img" src="{{ asset('build/images/Unknown.png') }}" alt="Vroum vrouuum">
                        {% endif %} #}
                        <div class="card-body">
                            <h5 class="card-title">{{ brand.name }}</h5>
                            <div class="d-flex justify-content-between align-item-center">
                                <a href="{{ path('cars_index', { 'id': brand.id }) }} class="btn btn-dark">Découvrir</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
